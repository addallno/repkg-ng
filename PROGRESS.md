# repkg-ng 开发进度

> 本文档记录 repkg-ng（Wallpaper Engine PKG 打包/解包工具）的当前状态、已知问题和未来规划。

---

## 当前进度

### 已完成的功能

#### Mobile 格式（format=5）解包支持
- 逆向分析了 Wallpaper Engine Android 版使用的自定义纹理格式 "Mobile"（format=5）
- 该格式本质是 **DXT1 压缩 + 恒定 Alpha=255**，存储为与 DXT5 相同的块结构
- 解包流程：LZ4 解压 → DXT 解压 → 输出 RGBA PNG
- 提取结果已验证与参考 PNG 100% 匹配（MAE=0.0，逐像素完全一致）

#### 打包功能（pack 命令）
- 支持目录 → PKG/MPKG（桌面版/Android版）
- 支持图片/视频 → TEX 纹理
- 自动识别输出格式（.mpkg → PKGM0019，.pkg → PKGV0005）

#### 格式转换（convert 命令）
- 支持 PKG ↔ MPKG 格式互转
- Android MPKG 包结构（PKGM0019/PKGM0016）完整支持

#### 其他改进
- 多框架支持（net8.0/net9.0），覆盖 Windows/Linux/macOS
- 视频纹理（TEXB0004）打包支持
- `.mpkg` 扩展名完整识别
- UTF-8 中文路径写入修复
- 图片裁剪/缩放崩溃修复

---

## 已知问题

### 大文件提取报错
- **现象**：超过一定大小的 `.tex` 文件在提取时报 `Mipmap byte count exceeds maximum size`
- **原因**：当前内部安全限制 `MaximumMipmapByteCount` 为 250 MB，压缩数据超过此值会被拒绝
- **影响**：包含高分辨率纹理或视频纹理的 MPKG 包无法完整提取
- **状态**：需要提高或移除此限制

### Mobile 格式打包方向未实现
- **现状**：提取（解包）已完全正确
- **不足**：打包（image → Mobile TEX）时仍输出 RGBA8888 格式，而非 DXT1 压缩
- **影响**：通过工具打包的 Mobile 格式文件体积会远大于原始文件
- **需要**：实现 DXT1 编码 + Mobile 块打包逻辑

### 视频纹理格式理解不完整
- **现状**：能识别和复制 MP4 视频纹理（TEXB0003/TEXB0004），但未完全理解其内部结构
- **影响**：无法从头创建新的视频纹理 TEX 文件（只能复制现有视频流）
- **格式特征**：format=0, Flags=34, 嵌入完整的 MP4 容器

### Flags 字段含义未完全解析
- **现状**：TexFlags 枚举只定义了部分标志位（IsGif, ClampUVs, IsVideoTexture 等）
- **缺失**：许多 Flags 值的用途未知，可能影响特定壁纸类型的正确处理

---

## 未来计划

### 短期（优先级高）

1. **提高大文件限制**
   - 增加 `MaximumMipmapByteCount` 上限或改为动态检测
   - 确保高分辨率壁纸（4K/8K）和视频壁纸能正常提取

2. **实现 Mobile 格式打包**
   - 添加 DXT1 编码器
   - 按照 Mobile 块格式（16字节块，恒定 FF 前缀 + DXT1 数据）写入
   - 使打包输出与原始文件体积一致

3. **补充单元测试**
   - Mobile 格式解包/打包的回归测试
   - 大文件处理测试
   - 边界条件测试（空文件、损坏文件）

### 中期

4. **完善视频纹理支持**
   - 支持从静态图片序列创建视频纹理
   - 支持视频转码（MP4 H.264 Baseline + yuv420p）
   - 视频尺寸自动检测

5. **Flags 枚举完善**
   - 通过更多样本逆向补全 TexFlags 各位的含义
   - 特别关注与动效、交互相关的标志

6. **错误处理改进**
   - 对未知格式给出更友好的提示
   - 提取失败时输出已成功处理的部分而非整体失败

### 长期

7. **场景编辑支持**
   - 场景 JSON 结构化编辑（不只是文本替换）
   - 材质参数调整界面

8. **性能优化**
   - 大文件流式处理（避免一次性加载到内存）
   - 并行解压/编码

---

## 技术备注

### Mobile 格式（format=5）解码方案

**数据结构**：
- 每个 16 字节块 = 1 个 4×4 像素块
- 字节 0-7：`FF 00 00 00 00 00 00 00`（恒定前缀，Alpha 通道填充）
- 字节 8-15：DXT1 压缩数据（RGB 颜色，无 Alpha）

**解码流程**：
1. 从 TEX 头部读取 TextureWidth（存储为字节宽度 = 像素宽度 × 4）
2. LZ4 解压 mipmap 数据
3. 将数据视为 DXT1 块进行解压
4. Alpha 通道统一设为 255（完全不透明）

**验证结果**：
- 提取尺寸：7200 × 4200
- 与参考 PNG 逐像素对比：MAE = 0.0，100% 匹配
